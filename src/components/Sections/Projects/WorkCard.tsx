import React from "react";
import styled from "styled-components";

function WorkCard() {
  return <StyledCard>WorkCard</StyledCard>;
}

const StyledCard = styled.div`
  width: 100%;
  height: 80px;
  background-color: black;
`;

export default WorkCard;
